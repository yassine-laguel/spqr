name: spqr_environment
dependencies:
- python=3.6
- pip
- pip:
        - cvxopt
        - numpy
        - scipy
        - scikit-learn
        - numba
        - torch # Only for EXPS
        - torchvision # Only for EXPS
        - torchaudio # Only for EXPS
        - scikit-image # Only for EXPS
        - imageio  # Only for EXPS
        - pandas # Only for EXPS
        - pandas # Only for EXPS
        - openml # Only for EXPS
